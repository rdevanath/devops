trigger:
- main

pool:
  name: Test

#build docker image
steps:
- script: docker build . --tag image_name:$(Build.BuildNumber)

# - script: docker build . -t myapp:$(Build.BuildNumber)
# - script: docker run -p 8080:8080 myapp:$(Build.BuildNumber)